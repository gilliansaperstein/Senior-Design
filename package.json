{
  "name": "snrdesign",
  "homepage": "https://juliamiller9.github.io/SnrDesign/index.html",
  "version": "1.0.0",
  "description": "communicating between HTML document and NodeJS",
  "main": "index.js",
  "dependencies": {
    "serialport": "^9.0.1",
    "socket.io": "^4.4.1",
    "gh-pages": "^2.0.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "npm test",
    "test": "echo \"Echo no test specified\" && exit 1",
    "start": "node app.js"
    
  }
}
